kind: AdaptiveDialog
description: High-fidelity SBAR (Situation, Background, Assessment, Recommendation) engine for clinical reports.
id: SBAR_Narrative_Engine
beginDialog:
  kind: OnRecognizedIntent
  id: Main
  actions:
    - kind: SendActivity
      activity: "р **SBAR SYNTHESIS INITIALIZED**: Constructing clinical narrative based on **EHR_INTEGRATION_BRIDGE.md** mappings..."
    
    # Situation
    - kind: SendActivity
      activity: "р **SITUATION**: ${Topic.PatientStatus} presenting with ${Topic.PrimaryDiagnosis}."
    
    # Background
    - kind: SendActivity
      activity: "р **BACKGROUND**: Longitudinal history indicates ${Topic.SurgicalHistory} with a Baseline Function of ${Topic.PriorLevelOfFunction}."
    
    # Assessment & Recommendation
    - kind: SendActivity
      activity: "впё **ASSESSMENT**: Demonstrated skilled need for ${Topic.Discipline} due to ${Topic.AuditRiskTrigger}. \n\nр **RECOMMENDATION**: Proceed with ${Topic.Frequency} for ${Topic.Duration} to meet functional target."
